<% unless request.xhr? %>
  <h1>Tagged with "<%= @tag %>" - <%= @feed_items.total_entries %> total <sup><%= link_to('rss', hub_tag_rss_path(@hub,u(@tag.name))) %> <%= link_to('atom', hub_tag_atom_path(@hub,u(@tag.name))) %></sup></h1>

<div class="tabs">
  <ul>
    <li><a href="#tagged_items">Items</a></li>
  </ul>
  <div id="tagged_items">
  <% end %>
    <%= will_paginate(@feed_items) %>
    <table class="hub_feed feed_items">
      <%= render :partial => 'shared/line_items/feed_item', :collection => @feed_items %>
    </table>
    <%= will_paginate(@feed_items) %>
<% unless request.xhr? %>
  </div>
</div>
<% end %>
