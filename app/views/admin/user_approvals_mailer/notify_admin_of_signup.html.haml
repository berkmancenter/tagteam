%p A new user from a domain that exists in the blacklist or is not included in the whitelist, #{@unapproved_user.email}, has signed up and is awaiting approval: #{link_to(admin_user_approvals_url, admin_user_approvals_url)}
