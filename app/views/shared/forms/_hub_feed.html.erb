<%= semantic_form_for(@hub_feed) do |f| %>
  <%= f.inputs do %>
    <%= f.input :title, :label => 'New Title', :hint => "This will inherit the title of the RSS feed if you don't enter anything." %>
    <%= f.input :description, :as => :text %>
  <% end %>
  <%= f.buttons %>
<% end %>
