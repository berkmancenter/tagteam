<%= semantic_form_for([@hub,@hub_feed]) do |f| %>
  <%= f.inputs do %>
    <%= f.input :title, :label => 'New Title' %>
    <%= f.input :description, :as => :text %>
  <% end %>
  <%= f.buttons %>
<% end %>
