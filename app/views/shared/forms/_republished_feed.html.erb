<%= semantic_form_for([@hub,@republished_feed]) do |f| %>
  <%= f.inputs do %>
    <%= f.input :hub_id, :as => :hidden %>
    <%= f.input :title %>
    <%= f.input :description, :as => :text %>
    <%= f.input :default_sort, :collection => RepublishedFeed::SORTS_FOR_SELECT %>
    <%= f.input :limit, :hint => 'How many items to show in this feed?'  %>
  <% end %>
  <%= f.buttons do %>
    <%= f.commit_button :label => (f.object.new_record?) ? 'Create new remixed feed' : 'Update this remixed feed'  %>
  <% end %>
<% end %>
