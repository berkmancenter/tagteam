<div id="homepage">
  <div id="left">
    <h1>TagTeam...</h1>
    <p>is an open-source RSS (and Atom) aggregrator with the power to:</p>
    <ul>
      <li>Aggregate anything that publishes RSS (Twitter, MediaWikis, blogs, Delicious, Zotero),</li>
      <li>Create custom feed remixes,</li>
      <li>Curate tags via a flexible filtering system,</li>
      <li>and republish everything as Atom, RSS, XML, and json.</li>
    </ul>
  </div>

  <div id="right">
    <div id="hub_list">
      <h2>Hubs</h2>
      <div class="search_results">
        <%= render :partial => 'shared/line_items/hub', :collection => @hubs %>
        <%= will_paginate(@hubs) %>
      </div>
      <% unless current_user.blank? %>
        <%= link_to(raw('<span class="inline ui-silk ui-silk-add"></span> New Hub'), new_hub_path, :class => 'new_object') %>
      <% end %>
    </div>
  </div>
  <div class="clear" style="float: left;"></div>

  <div id="item_list" class="search_results">
  </div>

</div>
