- content_for :top_panel do
  %h2.page-title My hubs
- content_for :tabs do
  = render partial: 'hub_tabs', locals: { active: 'mine' }
- content_for :tab_content do
  = render partial: 'hubs/hubs_list',
    locals: { hubs: @hubs,
      order: params[:order] || 'asc',
      sort: params[:sort] || 'title',
      path: 'my_hubs_path' }
